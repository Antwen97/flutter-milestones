name: flutter_milestones
description: "A good way to show the progress of your workflow. Detailed progress is provided using the boolean value on each sub-point."
version: 1.0.2
homepage: https://github.com/Antwen97/flutter-milestones

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  gradient_borders: ^1.0.0
  auto_size_text: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
